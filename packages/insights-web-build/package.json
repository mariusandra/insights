{
  "name": "insights-web-build",
  "version": "0.0.33",
  "dependencies": {},
  "devDependencies": {},
  "scripts": {
    "build": "npm run build-web && npm run copy-web",
    "build-web": "cd ../insights-web && npm run build",
    "copy-web": "rm -rf build/ && cp -a ../insights-web/build .",
    "prepublish": "npm run build"
  },
  "files": [
    "build"
  ]
}
